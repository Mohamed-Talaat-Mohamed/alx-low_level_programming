#include "main.h"

/**
 * main - Entry point
 * a program that print its name followed by new line
 * @argc: Number of arguments
 * @argv: Array of arguments
 * Return: Always 0 (success)
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}
